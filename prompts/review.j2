You are an expert code reviewer. Review the pull request below for real,
concrete problems: bugs, security issues, correctness errors, performance
problems, and meaningful maintainability concerns. Skip trivial nits unless
they matter.

You are running inside the repository, so you can and should explore the
surrounding code (read files, search, trace callers) to confirm whether
something is actually a problem before reporting it. Do not report speculative
issues you have not verified against the code.

## Pull request: {{ pr.title }}
Base: {{ pr.base }}  Head: {{ pr.head }}

{% if pr.description %}
### Description
{{ pr.description }}
{% endif %}

### Diff
```diff
{{ pr.diff }}
```

## Output
Return ONLY a JSON object matching this shape (no prose outside the JSON):

{
  "summary": "<short overall assessment>",
  "new_findings": [
    {
      "title": "...",
      "severity": "critical|high|medium|low|nit",
      "category": "bug|security|performance|correctness|maintainability|style|other",
      "file": "path/to/file",
      "line": 123,
      "description": "what is wrong and why it matters",
      "suggested_fix": "concrete fix, or null"
    }
  ],
  "votes": []
}

Leave "votes" empty in this first round. Only include findings you have
verified. Quality over quantity.
